name: Snyk SAST to Cortex Ingestion

on:
  push:
    branches: ["main"]

permissions:
  contents: read

jobs:
  snyk-ingestion:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Snyk CLI
        uses: snyk/actions/setup@master

      - name: Run Snyk Code (SAST)
        # We must use 'code test' to generate the SAST-only SARIF Cortex requires
        run: snyk code test --sarif-file-output=snyk.sarif
        continue-on-error: true 
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Ingest SARIF to Cortex XDR
        run: |
          # Replace 'YOUR_CORTEX_ASSET_ID' with the ID from the Cortex UI
          curl -X POST "https://api-neur-cxsiamp.xdr.eu.paloaltonetworks.com/public_api/appsec/v1/collectors/69bd401a1dc41618ccc66c34?repository_id=d8a825a41f66be5269d6d49d2a27cd00b56b6cf4328eb81c475978a77df525e0&branch=${{ github.ref_name }}" \
          -H "x-crtx-auth-id: ${{ secrets.CORTEX_TOKEN_ID }}" \
          -H "Authorization: ${{ secrets.CORTEX_API_KEY }}" \
          -H "Content-Type: application/json" \
          -d @snyk.sarif
        env:
          # These must be set in your GitHub Repo Settings > Secrets
          CORTEX_TOKEN_ID: ${{ secrets.CORTEX_TOKEN_ID }}
          CORTEX_API_KEY: ${{ secrets.CORTEX_API_KEY }}
